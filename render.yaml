services:
  - type: web
    name: communitiesqualifier
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: cd app_mantenimiento && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
